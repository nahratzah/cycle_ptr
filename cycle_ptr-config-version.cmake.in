set(PACKAGE_VERSION @CYCLE_PTR_VERSION@)

if("${PACKAGE_FIND_VERSION_MAJOR}" EQUAL "@CYCLE_PTR_VERSION_MAJOR@")
  if ("${PACKAGE_FIND_VERSION_MINOR}" EQUAL "@CYCLE_PTR_VERSION_MINOR@")
    set(PACKAGE_VERSION_EXACT TRUE)
  elseif("${PACKAGE_FIND_VERSION_MINOR}" LESS "@CYCLE_PTR_VERSION_MINOR@")
    set(PACKAGE_VERSION_COMPATIBLE TRUE)
  else()
    set(PACKAGE_VERSION_UNSUITABLE TRUE)
  endif()
else()
  set(PACKAGE_VERSION_UNSUITABLE TRUE)
endif()
